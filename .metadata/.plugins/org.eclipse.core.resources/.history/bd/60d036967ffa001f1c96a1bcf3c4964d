//SImple Tutorial (Week 4 )
package application;

import javafx.application.Application;
import javafx.stage.Stage;

public class Simple extends Application {
	
	// TODO declare and instantiate components
	// TODO write constructor and instantiate components

	public static void main(String[] args) {
		// launch the application
		launch(args);
	}

	// TODO Window management and layouts
	@Override
	public void start(Stage primaryStage) throws Exception {
		// set the title of the window
		primaryStage.setTitle("Simple tutorial");
		
		//set the default width of the window
		primaryStage.setWidth(500);
		
		//set the default height of the window
		primaryStage.setHeight(300);
		
		// TODO setup containers and layouts (Vbox + Hboxes)
		
		// TODO add components to containers
		
		// TODO make it look nice and show it
		
		//show the main method
		primaryStage.show();
		
		
	}

}
